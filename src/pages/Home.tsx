import HeroSection from '../components/HeroSection';
import AppLayout from '../layouts/AppLayout';

const Home = () => {
  return (
    <AppLayout>
      <HeroSection />
    </AppLayout>
  );
};

export default Home;
